name: Build MCC OS ISO

on:
  push:
    tags:
      - 'v*.*.*'
  workflow_dispatch:

jobs:
  build-iso:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - name: Checkout Code
        uses: actions/checkout@v4

      - name: Install build dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y live-build debootstrap

      - name: Inject Custom Packages (Simulated)
        run: |
          # In a real workflow, this would download the artifacts from the build-packages action
          # and place them in config/packages.chroot/ before running live-build
          mkdir -p config/packages.chroot
          echo "Packages injected successfully."

      - name: Build ISO
        run: |
          sudo lb config
          sudo lb build

      - name: Rename ISO
        run: |
          if [ -f live-image-amd64.hybrid.iso ]; then
            mv live-image-amd64.hybrid.iso mccos-amd64.iso
          fi

      - name: Upload ISO Artifact
        uses: actions/upload-artifact@v4
        with:
          name: mccos-iso
          path: mccos-amd64.iso

      - name: Create GitHub Release
        if: startsWith(github.ref, 'refs/tags/')
        uses: softprops/action-gh-release@v1
        with:
          files: mccos-amd64.iso
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
